# Dependencies
# This folder is huge and can be regenerated by running `npm install`.
# It should NEVER be committed to Git.
node_modules/

# Build output & Cache
# This is the compiled, optimized version of your site and the dev server cache.
dist/
dist-ssr/
.vite/

# Secrets & Environment Variables (CRITICAL)
# These files contain sensitive API keys and should never be committed.
.env
.env.local
.env.*.local

# Logs
# Log files are generated during runtime and are specific to a single session.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS-generated files
# These are metadata files created by macOS and Windows.
.DS_Store
Thumbs.db

# Editor directories and files
# These are local configuration files for your code editor.
# We are intentionally NOT ignoring extensions.json to recommend extensions to collaborators.
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?